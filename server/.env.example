# MongoDB connection string for the backend
MONGODB_URI=mongodb://127.0.0.1:27017/scrapdb

# JWT secret for auth token signing
JWT_SECRET=your_jwt_secret_here

# Optional port override (default is 5000)
PORT=5000
